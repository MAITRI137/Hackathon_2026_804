# Local development values only. Replace SESSION_SECRET before any non-local deployment.
DATABASE_URL="postgresql://peoplepay:peoplepay@localhost:5432/peoplepay?schema=public"
PORT=3000
NODE_ENV=development
SESSION_SECRET="replace-with-a-unique-at-least-32-character-secret"
APP_ORIGIN="http://localhost:5173"
SMTP_HOST="smtp.example.com"
SMTP_PORT=587
SMTP_USER="username"
SMTP_PASS="password"
SMTP_FROM="PeoplePay360 <no-reply@example.com>"

